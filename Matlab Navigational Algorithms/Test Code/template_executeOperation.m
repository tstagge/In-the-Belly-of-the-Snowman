void executeOperation(byte op, byte pow)
{
    switch(op)
    {
        case 1:
        
            break;
        case 2:
            
            break;
        case 3:
            
            break;
        default:
            printf('Error! Not a valid operation number: %d', op);
    
    }
}